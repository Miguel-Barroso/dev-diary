# dev-diary

A working notebook of the systems I build, break, and fix at home.

Self-taught developer notes from a decade of running things on my own hardware — NAS, mini PCs, Raspberry Pis, laptops, and one extremely opinionated ThinkPad. The entries are written as I go, so they're not polished tutorials. They're the record of decisions, dead ends, and the fixes that actually worked. If something here helps you skip a few hours of debugging, that's the point.

---

## Index

### Workstations
- [`macbook-pro-m4.md`](macbook-pro-m4.md) — Flutter dev environment with FVM, debugging a 500 ms VPN-induced latency issue on macOS, VSCodium autocomplete recovery, and why the machine holding every SSH key is the one nothing else is allowed to reach
- [`x1e-pop-os.md`](x1e-pop-os.md) — *(retired 2026-09)* ThinkPad X1 Extreme on Pop!_OS: PIA and Tailscale coexistence, UFW hardening, fixing slow `sudo`, systemd-automounted NAS shares over Tailscale — the dual boot is gone and that SSD is now Astromeda's CI drive
- [`x1e-win-11.md`](x1e-win-11.md) — the same ThinkPad on Windows 11: a disk Windows had encrypted by itself and then left with **no key protectors at all**, so `100% encrypted` meant nothing and a 2020 recovery key couldn't have worked even in principle; getting the TPM bound to PCR7 instead of the firmware-brittle fallback, and the password-manager sync bug that faked a successful save of the new recovery key; a free-space wipe I declared dead three times that had completed every time, because the completion events live in a different event provider than the one I was reading; two scheduled tasks that never went missing; QNAP's camera client writing the NAS admin password to its own logs in cleartext on every launch, and proving the same password had sat there unchanged for three and a half months; pulling out a signed ring-0 driver that was quietly undoing the whole boot-integrity story — and a Defender scan I thought I'd verified with a probe that printed success unconditionally; boot hangs traced to an iGPU I'd disabled in the BIOS for an OS I no longer had; a CPU cap on WSL I was talked out of, correctly; a camera client whose "missing" service was a watchdog that tripled it at 668% CPU; PIA polling the whole process table ten times a second; striped Firefox uploads that were a privacy setting, not the GPU; flashing the NVMe from Windows because the vendor's boot ISO could never have booted; eight instant power-offs that turned out to be a power-path fault rather than software, and a battery whose capacity "went up"; and tuning it as a desk machine, including the power-plan setting that does nothing while another one is at 0

### Self-hosted infrastructure
- [`qnapbox66.md`](qnapbox66.md) — Vaultwarden on a QNAP NAS with Caddy reverse proxy, Cloudflare DNS-01 certificates, the hardening pass that came after I caught myself doing something dumb with secrets, key-only SSH on a box where `PermitRootLogin no` would lock you out, using an *empty* kernel crash buffer to prove a reboot was a power event rather than a crash, and a bridging loop through the NAS's virtual switch whose second leg turned out to be a mesh satellite's wireless backhaul rather than any cable — plus the DHCP lease I found pinned to the MAC of an adapter I'd stopped using, a six-year shutdown trend that only made sense once I noticed the log had outlived the chassis it was written on, and a replacement PSU that matched the original on every digit of the model number and not on the one thing that mattered — the cable harness
- [`astromeda-log.md`](astromeda-log.md) — a Windows gaming PC that became the busiest host in the house: multiple Minecraft Bedrock servers in Docker with Task Scheduler backups, the one-line "fix" I recommended here for months that was quietly making a server unjoinable, and the 2026-09 pass that moved the Hyper-V CI VM, Docker's data root and the WSL2 distro off an SMR drive onto a dedicated NVMe — including the disk that wouldn't attach until three Windows services let go of it, BitLocker encrypting the volume at the wrong cipher two seconds before I could pick one, a 25% "regression" that turned out to be an SSD's SLC cache rather than anything I'd configured, and going from one CI runner to three, which promptly proved that half the pipeline had assumed one job at a time; then the 2026-09-25 day on which the Minecraft lag turned out to be an army of 586 drowned nobody had counted, and a 2020 BIOS that could only be left via the Flashback button — after a USB stick bricked the board on the first try; and that evening's freezes, traced with a once-a-second gametime sampler to the tick thread blocking on a 9p mount, which moved both worlds onto Docker volumes and turned up three things the NVMe move had left pointing at the empty drive: two weeks of silently failed backups, stale Defender exclusions, and a VHDX owned by the wrong account
- [`macmini-2012-log.md`](macmini-2012-log.md) — 2012 Mac Mini repurposed as a 24/7 cat-café livestream host and network-wide AdGuard DNS server, with OBS hardware encoding, AdGuard DHCP takeover, what it takes to put the household resolver behind a deny-by-default policy without taking the house offline, closing an RTMP ingest that was open by default on both play and publish, finding postfix had been silently dead for weeks while wiring SmokePing's alert mail through a real relay, and later catching that same SmokePing running two-day-old config against an address I'd retired — while a bounce loop that emits no bounces quietly ate every cron mail on the box

### Raspberry Pi
- [`rbpi3-log.md`](rbpi3-log.md) — Pi 3 surveillance camera with a custom systemd-driven recovery script that revives the Broadcom WiFi chip when it hangs, a tidy-up that turned into a lesson about mistaking leftover config for a working setup, why a monitoring tool and a live stream can disagree about whether the box is up and both be right, a retraction of that conclusion that turned out to be wrong itself, and retiring the WiFi radio on a box whose own watchdog was built to reboot it back on
- [`rbpi4-catcam.md`](rbpi4-catcam.md) — Pi 4 with a USB webcam for a public cat livestream: hardware H.264 (`h264_v4l2m2m`) pushed as RTMP over 5 GHz WiFi into OBS, replacing the old MJPEG + XComposite screen-capture hack
- [`rbpi4-cctv.md`](rbpi4-cctv.md) — migrating mjpg-streamer from Pi 3 to Pi 4, plus an FFmpeg-to-RTMP relay path

### Email
- [`email-triage.md`](email-triage.md) — ten mail accounts that turned out to have **zero** filters between them, and the measurement that put 55% of 11,170 inbox messages down as machine-generated; why I threw away a finished set of Thunderbird filters once I asked whether they'd work on my phone, and moved the whole thing to server-side Sieve instead; the Google Business Profile mail that carries an unsubscribe header while containing 176 real customer enquiries, and the bank pattern that matched a human mid-thread because I matched a substring instead of a domain — two near-misses that produced the rule the design now rests on, that mail which looks human never moves; why enumerating WordPress site titles is a losing game, why `imaplib` has no `.examine()`, why a FETCH without an explicit `UID` silently keys your lookup table by sequence number and turns a protection into a no-op, the ~400-message ceiling above which Migadu's IMAP proxy stops answering entirely and the wrong first diagnosis I gave it, the compaction preference Thunderbird 153 quietly renamed so that every older instruction does nothing, and the DMARC folder rule that came out correct but almost completely idle because three of five domains had never actually asked for reports

### Web and games
- [`miguelbarroso.com.md`](miguelbarroso.com.md) — `fswatch` and `rsync` continuous sync to a SiteGround WordPress install, and why I eventually abandoned the approach
- [`nekocafetime.com.md`](nekocafetime.com.md) — WooCommerce and WPML variation-translation troubleshooting

---

## How to read this

Entries are dated where it mattered to me at the time. Some are stream-of-consciousness, some are clean step-by-step writeups. Treat them as a journal, not a manual.

Everything that needs a real credential, real internal IP, or real account identifier has been redacted with angle-bracketed placeholders like `<YOUR_PASSWORD>` or `<pi-lan-ip>`. If you spot something I missed, please open an issue.
